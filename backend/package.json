{
  "name": "acheez",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init:local": "wrangler d1 execute acheez-db --local --file=./schema.sql",
    "d1:init:remote": "wrangler d1 execute acheez-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250224.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.111.0"
  }
}
